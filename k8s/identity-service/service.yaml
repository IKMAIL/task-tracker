apiVersion: v1
kind: Service
metadata:
  name: identity-service
  namespace: task-tracker
  labels:
    app: identity-service
spec:
  selector:
    app: identity-service
  ports:
    - name: http
      port: 3001
      targetPort: 3001
