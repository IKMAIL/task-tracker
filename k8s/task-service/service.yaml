apiVersion: v1
kind: Service
metadata:
  name: task-service
  namespace: task-tracker
  labels:
    app: task-service
spec:
  selector:
    app: task-service
  ports:
    - name: http
      port: 3002
      targetPort: 3002
