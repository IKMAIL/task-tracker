apiVersion: v1
kind: Service
metadata:
  name: web-frontend
  namespace: task-tracker
  labels:
    app: web-frontend
spec:
  type: NodePort
  selector:
    app: web-frontend
  ports:
    - name: http
      port: 3005
      targetPort: 3005
      nodePort: 30005
